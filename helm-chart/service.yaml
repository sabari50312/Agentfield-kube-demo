apiVersion: v1
kind: Service
metadata:
  name: agentfield-test-service
spec:
  type: NodePort
  selector:
    app: agentfield-test
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30008
